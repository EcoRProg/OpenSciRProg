---
title: "Importar Datos Chla"
author: "L.A.S."
date: "July 17, 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Importa Chla

```{r importaChla}

chla <- read.delim("Data/Clorofila.txt", stringsAsFactors = FALSE)
chla #se ve el objeto
str(chla) #da la estructira de los datos
colnames(chla) #da el nombre de las columnas
rownames(chla) #da el nombre de las filas
nrow(chla) #da el numero de filas

install.packages("lubridate")
require(lubridate)
source("R/function.r")
chla <- read.delim("Data/Clorofila.txt", stringsAsFactors = FALSE)
 chla <- fixChlorophyllData(chla)
chla
str(chla)

```
